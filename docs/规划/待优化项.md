删除按钮为软删除，不应该直接操作实际的文件，而是将其在软件中的扫描记录删除，并确保下次扫描不会扫描到该目录  --完成
增加暗黑模式  --完成
优化前端显示样式 --完成
工作流节点在点击回退后前端需要有响应或者显示
创建通用弹窗组件，要求符合现有前端设计  --完成
工作流节点报错保留运行历史记录，可保证下次运行同样工作流时，直接从报错节点开始，而不是从头开始
工作流完成后，目录移动完成，那么系统中还会保留工作流记录和文件目录记录吗，能够回退吗
工作流节点报错请弹窗显示，而不是显示在节点内  --完成
节点是否可以合并成一个原子节点，拆分目录到最小叶子目录
folder-picker节点增加配置项，可从数据库中已保存的文件夹中选择   --完成
扫描的文件夹明明有视频和图片，但是界面显示为0B --完成
媒体文件夹列表现在只要修改一个文件夹的状态，整个列表都会刷新，需要移除 --完成
在工作流编辑界面添加了新节点和连线，必须保存工作流后才能运行，否则运行会忽略新加入的节点 --完成
重新设计节点，确保每个节点都有用处
folder-picker 输出了 2 个路径，但 path 口只给了第一个：F:\p\欧美\compilation\baebz
查看工作流引擎的设计，建立相应的节点设计规范，要求每个节点都有校验，保证数据的输入输出流没有问题，不能接受空输入，杜绝假成功的情况  --完成
移动优化：
    视频需要按照目录结构移动，具体指：所有A目录下的视频，按照路径重命名，移动到一个目录后，将整个目录重命名为A后，再移动到target/video目录下
    图片比较特殊，分情况讨论
        A目录没有叶子目录，全部为图片，创建同名文件夹，将全部图片放在其中，压缩，将A目录移动到目标图片目录
        A目录存在多个分类判定为图片目录的叶子目录，则分别压缩为压缩包，将A目录只能移动到目标图片目录
        A目录存在多个分类判定为图片目录的叶子目录，还存在没有叶子目录的混杂目录，则需要将其中的图片移动出来，单独打包，并重命名，平铺移动到目标图片目录/A目录下
任务历史需要能看到失败原因，而不是只显示失败，需要包含跳转到审计日志的按钮，而审计日志需要足够详细，显示失败的具体原因  --完成
媒体文件夹应当显示每个文件夹是否经过工作流的分类和处理流程  --完成
启动工作流后的弹窗中的当前运行卡片不能实时显示运行情况，且点击查看完整明细后跳转到的不是相应工作流的任务历史展开信息，而是计划任务页
任务历史页将类型列变为工作流名称列
工作流可视化编辑界面，支持将连线手动从一个端点断开并拖动到其他端点进行连接
在处理过文件夹并重新扫描后，之前的文件夹记录会被新的文件夹记录替代，导致回退和历史记录不可用
在工作流界面运行过分类和处理工作流后，媒体文件夹的状态未更新，文件夹操作时间线也并未更新
文件夹操作时间线增加追踪文件夹大小和文件数量，减少不必要信息的显示重心
在工作流可视化编辑界面运行失败报错，外面直接启动运行成功不报错，两者的运行结果不一致
folder-picker禁止多选，为单选
梳理项目中使用配置的所有路径格式，并简化，根目录为docker部署的文件系统根目录路径，只跟docker compose中的设置有关，应用启动后不能更改，应用运行时只需要两种路径：扫描目录和输出目录
移动节点后接缩略图节点，实现移动后在创建缩略图的功能，但是启动后报错，缩略图节点显示空输入
创建新节点，用于解决category router mixed-leaf分类最小叶子混合目录再分类视频图片的问题，输入mixed-leaf分类最小叶子混合目录，并进行video和photo两路输出的节点。功能为将最小叶子混合目录进行分类输出
报错修复：
工作流#通用处理流程.json接入一个只有纯视频的目录时报错
low.thumbnail-node	failed	thumbnail-node.Execute: no direct video file found in "F:\\p\\4K\\jamie jett"	
job=ccddf8ff-72dd-4056-8f2a-fc1b4958a5ac
run=d561d58b-8ffe-45cd-9a46-0531e3770613
nodeRun=eeab4120-3cc3-426e-959c-8ab83adaa75e
node=thumbnail-node-12
type=thumbnail-node
节点执行失败：移动节点
节点 ID：move-node-13
错误详情
skip_reason=empty_input
------------完成-------------------
优化启动工作流弹窗，显示在视角正中心而非页面的正中心  -完成
A文件夹处理过后，被处理过后的文件出现在了媒体文件夹的列表中，大小为0B，被处理过后的文件不应该出现，为什么会出现
给媒体文件夹列添加修改时间排序，大小排序
A文件夹在被分类和处理过后，没有任何文件夹文件流向，显示暂无文件级去向数据，当前仅展示摘要与时间线信息
媒体文件夹当输出目录为扫描目录的子目录时，媒体文件夹依旧出现了输出目录
---------------完成
为什么文件夹处理完成后，会在backend/.archive文件夹下保留文件  --完成
带视频的文件夹A在处理完以后，媒体文件夹会多出一个Videos文件夹，保留了文件夹A视频的目录分组文件流向图，但是文件夹A没有目录分组文件流向图，我有两个问题：1.为什么会多出一个Videos文件夹？2.为什么该Videos文件夹保留了文件夹A没有的目录分组文件流向图和其他信息？  
执行历史没有分页，无法显示完整记录
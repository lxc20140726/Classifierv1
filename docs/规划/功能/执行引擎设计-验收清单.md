# 执行引擎设计对照验收清单

> 对照文档：`docs/功能/执行引擎设计.md`
> 更新时间：2026-03-27
> 状态说明：`已完成` / `部分完成` / `未完成`

---

## 1. 总体结论

- 已完成：执行引擎运行时已切换到 `TypedValue + 命名端口`，分类链已改为批量并行架构，`SourceDir` 启动、DB 持久化 resume、TypeRegistry 序列化、Lazy-aware skip 等核心能力已落地。处理链 single/list 兼容残留已清除，统一为 list-only 语义。节点 schema 中 `InputPorts`/`OutputPorts` 兼容字段和 `NodeSchemaPort` 别名已全部清理。所有设计稿验收用例已补齐。
- 部分完成：旧 `move` 节点仍保留为向后兼容注册项。
- 未完成：无。

---

## 2. 问题清单对照（P1-P10）

| ID | 设计问题 | 状态 | 证据 | 说明 |
|---|---|---|---|---|
| P1 | scanner 输出 token 被 runner 丢弃 | 已完成 | `backend/internal/service/node_folder_tree_scanner.go:81`, `backend/internal/service/workflow_runner.go:49`, `backend/internal/service/workflow_runner.go:1032` | scanner 已输出 `trees` 批量端口；runner 已按命名端口读取 typed cache。 |
| P2 | 5 个分类节点未注册 | 已完成 | `backend/internal/service/workflow_runner.go:181` | 5 个分类链节点已在 `NewWorkflowRunnerService` 中注册。 |
| P3 | `hasNilRequiredInput` 与 Lazy 语义冲突 | 已完成 | `backend/internal/service/workflow_runner.go:740`, `backend/internal/service/workflow_runner_test.go` | 已改为 Lazy-aware skip；相关行为已有测试覆盖。 |
| P4 | `subtree-aggregator` 未实现 bottom-up 聚合 | 已完成 | `backend/internal/service/node_subtree_aggregator.go:53`, `backend/internal/service/workflow_types.go:34`, `backend/internal/service/node_subtree_aggregator_test.go:151` | 已实现递归 `aggregateTree(...)`、`mixed` 推导和 `[]ClassifiedEntry` 子树结构，并补了递归 mixed / 强信号覆盖测试。 |
| P5 | 类型系统装饰化，JSON 往返擦除类型 | 已完成 | `backend/internal/service/port_types.go:8`, `backend/internal/service/workflow_runner.go:1277`, `backend/internal/service/workflow_runner.go:1317` | 运行时、缓存、输出持久化已切到 `TypedValueJSON`；旧格式仅读取边界兼容。 |
| P6 | `WorkflowRun.folder_id NOT NULL` 与分类管道冲突 | 已完成 | `backend/internal/service/workflow_runner.go:223`, `backend/internal/repository/workflow_run_repo.go`, `backend/internal/db/migrations/011_engine_v2.sql` | 已支持 `SourceDir` 启动，无需预置 `FolderIDs`。 |
| P7 | Resume 状态纯内存存储 | 已完成 | `backend/internal/service/workflow_runner.go:381`, `backend/internal/repository/node_run_repo.go`, `backend/internal/db/migrations/011_engine_v2.sql` | `resume_data` 已持久化到 DB，Resume 从 DB 恢复。 |
| P8 | 批处理语义分散，无统一规范 | 已完成 | `backend/internal/service/node_category_router.go`, `backend/internal/service/node_rename_node.go`, `backend/internal/service/node_move_node.go`, `backend/internal/service/node_thumbnail_node.go`, `backend/internal/service/node_compress_node.go` | 处理链已全面切到 list-only 语义：移除 `isList` 分支，所有处理节点始终输出 `PortTypeProcessingItemList`。`categoryRouterExtractItems` / `categoryRouterToItems` 已去除 `isList` 返回值。 |
| P9 | `ext-ratio-classifier` 绕过图数据流 | 已完成 | `backend/internal/service/workflow_runner.go:1488` | 已改为读取输入端口 `trees`，不再调用 `fs.ReadDir`。 |
| P10 | 两套 move-node 并存，旧实现有死代码 | 部分完成 | `backend/internal/service/node_move_node.go:13`, `backend/internal/service/workflow_runner.go` | `move-node` 已成为 typed runtime 主路径，且 phase4 rollback 已实现；旧 `move` 保留为向后兼容注册项。 |

---

## 3. 设计目标对照

| 设计目标 | 状态 | 证据 | 说明 |
|---|---|---|---|
| 强类型端口系统（`PortType` + `TypedValue`） | 已完成 | `backend/internal/service/port_types.go:8`, `backend/internal/service/workflow_runner.go:32` | 运行时已切换到 typed inputs/outputs。 |
| 命名端口替代数字索引 | 已完成 | `backend/internal/service/workflow_runner.go:1191`, `backend/internal/service/workflow_runner.go:1011` | 旧索引仅在 parse 阶段归一化为 `SourcePort`。 |
| 批量 Token 语义（`[]T`） | 已完成 | `backend/internal/service/node_category_router.go`, `backend/internal/service/node_rename_node.go`, `backend/internal/service/node_move_node.go`, `backend/internal/service/node_thumbnail_node.go`, `backend/internal/service/node_compress_node.go` | 处理链已完全批量化；`isList` 分支已清除，所有处理节点一律输出列表类型。 |
| `shouldSkipNode` 尊重 `Lazy` | 已完成 | `backend/internal/service/workflow_runner.go:1041` | 已落地。 |
| 注册全部 14 个节点 | 已完成 | `backend/internal/service/workflow_runner.go:181` | 注册表完整（含 `audit-log`）。 |
| 并行分类器架构 | 已完成 | `backend/internal/service/workflow_phase3_e2e_test.go`, `backend/internal/service/workflow_seed.go` | 分类链已改为 scanner 扇出到多个分类器。 |
| `subtree-aggregator` 真正 bottom-up 聚合 | 已完成 | `backend/internal/service/node_subtree_aggregator.go:53`, `backend/internal/service/workflow_types.go:34`, `backend/internal/service/node_subtree_aggregator_test.go:151` | 已实现递归子树聚合、统一/混合分类推导，以及高置信度直接信号覆盖。 |
| `ext-ratio-classifier` 读取输入端口数据 | 已完成 | `backend/internal/service/workflow_runner.go:1488` | 已完成。 |
| `TypeRegistry` 类型感知序列化/反序列化 | 已完成 | `backend/internal/service/port_types.go:33`, `backend/internal/service/workflow_runner.go:1277` | 已接入 runner。 |
| `StartWorkflowJobInput` 支持 `SourceDir` | 已完成 | `backend/internal/service/workflow_runner.go:21` | 已完成。 |
| Resume 数据持久化到 `node_runs.resume_data` | 已完成 | `backend/internal/repository/node_run_repo.go`, `backend/internal/service/workflow_runner.go:381` | 已完成。 |
| 废弃旧 `move`，统一 `move-node` | 部分完成 | `backend/internal/service/workflow_runner.go:193`, `backend/internal/service/node_move_node.go` | 新链路用 `move-node`，且其 rollback 已落地；旧 `move` 保留为向后兼容注册项（`TestEngineV2_AC_COMPAT2` 有回归覆盖）。 |
| 节点 schema `InputPorts`/`OutputPorts` 已移除 | 已完成 | `backend/internal/service/workflow_runner.go:65`, 全体节点文件 | `NodeSchemaPort = PortDef` 别名已删除；`NodeSchema.InputPorts`/`OutputPorts` 字段已删除；所有节点统一使用 `Inputs`/`Outputs []PortDef`（JSON 标签仍为 `input_ports`/`output_ports` 以保持前端兼容）。 |

---

## 4. 章节级对照

### 4.1 类型系统（第三节）

- 已完成
  - `PortType`、`TypedValue`、`TypedValueJSON`、`TypeRegistry` 已落地：`backend/internal/service/port_types.go:8`
  - `ClassificationSignal.SourcePath` 已落地：`backend/internal/service/workflow_types.go:16`
  - 运行时输出 / cache / DB 序列化已接入 typed map：`backend/internal/service/workflow_runner.go:1277`

### 4.2 节点接口规范（第四节）

- 已完成
  - `NodeExecutionInput.Inputs map[string]*TypedValue`：`backend/internal/service/workflow_runner.go:32`
  - `NodeExecutionOutput.Outputs map[string]TypedValue`：`backend/internal/service/workflow_runner.go:49`
  - `NodeSchema` 已清理：移除 `InputPorts`/`OutputPorts` 兼容字段和 `NodeSchemaPort = PortDef` 别名，统一使用 `Inputs`/`Outputs []PortDef`

### 4.3 图 JSON 格式（第五节）

- 已完成
  - 新 runtime 已按 `SourcePort` / `TargetPort` 工作：`backend/internal/service/workflow_runner.go:1191`
  - 旧整数索引兼容保留在 graph read 边界：`backend/internal/repository/models.go:223`
  - 默认 seed 工作流已改为纯命名端口图：`backend/internal/service/workflow_seed.go`
  - seed 测试已断言不再残留 `output_port_index` / numeric edge ports：`backend/internal/service/workflow_seed_test.go`

### 4.4 图验证规则（第六节）

- 已完成
  - `validateGraph(...)` 已落地：`backend/internal/service/workflow_graph_validator.go:27`
  - 已接入工作流保存路径：`backend/internal/handler/workflow_def.go`
  - 已覆盖类型不匹配、缺失必填端口、未知节点类型、环依赖、边/input 一致性等规则：`backend/internal/service/workflow_graph_validator_test.go`

### 4.5 执行引擎算法（第七节）

- 已完成
  - 输出缓存切到 `map[nodeID][portName]TypedValue`：`backend/internal/service/workflow_runner.go`
  - `resolveInputs` 已按命名端口解析：`backend/internal/service/workflow_runner.go:1011`
  - Resume 从 DB 恢复：`backend/internal/service/workflow_runner.go:381`
  - `normalizeGraphPortReferences` 已实现：`backend/internal/service/workflow_runner.go:1191`
  - `parseNodeOutputsForSchema(...)` 和 `parseTypedNodeOutputs(...)` 仍保留旧格式读取兼容（符合"读边界兼容"设计）

### 4.6 完整节点注册表（第八节）

- 已完成
  - 17 个节点（含向后兼容 `move`）均可运行于当前 registry：`backend/internal/service/workflow_runner.go:181`
  - 所有节点 schema 已迁移到 `Inputs`/`Outputs []PortDef`，legacy port 描述已更新

### 4.7 数据流图（第九节）

- 分类管道：已完成
  - 并行分类链已落地
  - `subtree-aggregator` 已完成设计稿中的 bottom-up mixed 聚合
- 处理管道：已完成
  - typed named-port runtime 已打通
  - `category-router` 输出空列表而非 `nil`
  - 处理链 single/list 兼容残留已全部清除，统一 list-only

### 4.8 数据库 Schema 增量（第十节）

- 已完成
  - `resume_data`、`source_dir`、nullable `workflow_runs.folder_id` 已落地
  - 实际迁移文件名为 `011_engine_v2.sql`，不是设计稿中的 `006_engine_v2.sql`
  - 证据：`backend/internal/db/migrations/011_engine_v2.sql`

### 4.9 迁移路径 / 兼容设计（第十一、十二节）

- 已完成
  - 旧 graph 数字端口索引兼容：`backend/internal/repository/models.go:223`, `backend/internal/service/workflow_runner.go:1191`
  - 旧 `output_json` 双格式 rollback 读取：`backend/internal/service/node_thumbnail_node.go`, `backend/internal/service/node_compress_node.go`
  - 分类管道 `folder=nil` rollback 安全处理：`backend/internal/service/workflow_runner.go:428`
  - `phase4MoveNodeExecutor.Rollback()` 已实现 typed/legacy 输出兼容回滚
  - 旧 `move` 节点保留为向后兼容注册项，`TestEngineV2_AC_COMPAT2_LegacyScanAndMoveJobRegression` 覆盖

---

## 5. 验收标准对照（第十三节）

| ID | 验收项 | 状态 | 依据 |
|---|---|---|---|
| AC-TYPE-1 | 类型不匹配连线保存时报错 | 已完成 | `backend/internal/service/workflow_graph_validator.go`, `backend/internal/service/workflow_graph_validator_test.go` |
| AC-TYPE-2 | Required 端口未连接时报错 | 已完成 | `backend/internal/service/workflow_graph_validator.go`, `backend/internal/service/workflow_graph_validator_test.go` |
| AC-TYPE-3 | Required + Lazy 矛盾配置拒绝 | 已完成 | `backend/internal/service/workflow_graph_validator.go`, `backend/internal/service/workflow_graph_validator_test.go` |
| AC-TYPE-4 | 未知节点类型保存时报错 | 已完成 | `backend/internal/service/workflow_graph_validator.go`, `backend/internal/service/workflow_graph_validator_test.go`, `backend/internal/handler/workflow_def_test.go` |
| AC-TYPE-5 | 环形依赖保存时报错 | 已完成 | `backend/internal/service/workflow_graph_validator.go`, `backend/internal/service/workflow_graph_validator_test.go` |
| AC-EXEC-1 | Lazy nil 不跳过节点 | 已完成 | `backend/internal/service/workflow_runner_test.go` 已覆盖 lazy skip 语义 |
| AC-EXEC-2 | Required nil 跳过节点 | 已完成 | `backend/internal/service/workflow_runner_test.go` 已覆盖 strict required skip |
| AC-EXEC-3 | 旧数字索引 GraphJSON 兼容执行 | 已完成 | `backend/internal/service/workflow_runner_test.go` 命名端口/索引兼容测试 + `normalizeGraphPortReferences` |
| AC-EXEC-4 | TypeRegistry JSON 往返不丢类型 | 已完成 | `backend/internal/service/port_types_test.go`, `backend/internal/service/workflow_runner_test.go` |
| AC-CLASS-1 | 单次 WorkflowRun 扫描 N 个目录并全部分类 | 已完成 | `backend/internal/service/workflow_phase3_e2e_test.go:TestPhase3WorkflowE2E_BatchSourceDirClassification`（3 目录，验证全部正确分类） |
| AC-CLASS-2 | 关键词优先于 ext-ratio | 已完成 | `backend/internal/service/workflow_phase3_e2e_test.go:TestEngineV2_AC_CLASS2_KeywordPriorityOverExtRatio`（"漫画合集"+视频文件，kw conf=1.0 胜出 ext-ratio conf=0.85） |
| AC-CLASS-3 | mixed bottom-up 递归聚合 | 已完成 | `backend/internal/service/node_subtree_aggregator.go:138`, `backend/internal/service/node_subtree_aggregator_test.go:151`, `backend/internal/service/node_subtree_aggregator_test.go:197` |
| AC-CLASS-4 | manual-classifier DB 持久化 + 重启后 resume | 已完成 | `workflow_runner.go` 持久化逻辑 + 现有 resume 相关测试 |
| AC-PROC-1 | 完整处理链 compress → rename → move | 已完成 | `backend/internal/service/workflow_runner_test.go:TestEngineV2_AC_PROC1_ProcessingChainRenameAndMove`（category-router→rename→move，验证目标路径） |
| AC-PROC-2 | category-router 正确路由各端口 | 已完成 | `backend/internal/service/node_phase4_processing_test.go` |
| AC-PROC-3 | 空列表不触发节点跳过 | 已完成 | `backend/internal/service/node_category_router.go`, `backend/internal/service/node_phase4_processing_test.go` |
| AC-ROLL-1 | `move-node` Rollback 把文件移回原路径 | 已完成 | `backend/internal/service/node_move_node.go:146`, `backend/internal/service/node_phase4_processing_test.go:286`, `backend/internal/service/workflow_runner_test.go:876` |
| AC-ROLL-2 | `thumbnail-node` Rollback 删除缩略图 | 已完成 | `backend/internal/service/node_phase4_processing_test.go:TestThumbnailNodeExecutorRollbackRemovesFilesAndClearsCover`（旧格式）+ `TestEngineV2_AC_ROLL2_ThumbnailNodeRollbackTypedFormat`（typed 格式） |
| AC-ROLL-3 | `compress-node` Rollback 删除归档 | 已完成 | `backend/internal/service/node_phase4_processing_test.go:TestCompressNodeExecutorRollbackRemovesGeneratedArchives`（旧格式）+ `TestEngineV2_AC_ROLL3_CompressNodeRollbackTypedFormat`（typed 格式） |
| AC-ROLL-4 | 多节点逆序 Rollback | 已完成 | `backend/internal/service/workflow_runner_test.go:TestEngineV2_AC_ROLL4_MultiNodeReverseRollback`（source→move1→rename→move2，rollback 后文件回到初始路径） |
| AC-PEND-1 | resume_data 写入 DB 且重启后可恢复 | 已完成 | `backend/internal/service/workflow_runner.go:381`, `backend/internal/repository/node_run_repo.go` |
| AC-COMPAT-1 | 旧 `output_json` 的 Rollback 仍可工作 | 已完成 | `backend/internal/service/node_phase4_processing_test.go:TestEngineV2_AC_COMPAT1_LegacyOutputJsonRollbackCompat`（compress + thumbnail 旧数组格式注入验证） |
| AC-COMPAT-2 | 旧 scan/move Job 不受影响 | 已完成 | `backend/internal/service/workflow_phase3_e2e_test.go:TestEngineV2_AC_COMPAT2_LegacyScanAndMoveJobRegression`（旧 `move` 节点 + FolderIDs 模式，成功完成并更新路径） |

---

## 6. 剩余尾项

### 已全部收口

所有高、中、低优先级尾项已处理完毕：
- 处理链 single/list 兼容残留 → 已清除（P8）
- 节点 schema `InputPorts`/`OutputPorts` 兼容字段与 `NodeSchemaPort` 别名 → 已删除
- 设计稿等价验收用例 → 已全部补齐（AC-CLASS-2 / AC-PROC-1 / AC-ROLL-2~4 / AC-COMPAT-1~2）

### 仍保留的向后兼容项（不视为缺陷）

- 旧 `move` 节点注册（`moveNodeExecutor`）：保留以兼容已有工作流定义
- 分类链节点 `folder`/`signal` 等兼容端口：保留以兼容旧图 JSON（读边界兼容，不影响新图）
- `parseNodeOutputsForSchema(...)` / `parseTypedNodeOutputs(...)` 旧格式读取路径：保留以兼容历史 `output_json`

---

## 7. 当前可确认的验收证据

- typed runtime 已落地：`backend/internal/service/workflow_runner.go:32`
- `validateGraph(...)` 已落地并接入保存路径：`backend/internal/service/workflow_graph_validator.go:27`, `backend/internal/handler/workflow_def.go`
- TypeRegistry 已接入运行时：`backend/internal/service/workflow_runner.go:1277`
- 图索引 → 命名端口归一化：`backend/internal/service/workflow_runner.go:1191`
- 批量分类链已落地：`backend/internal/service/node_folder_tree_scanner.go:81`
- 递归 bottom-up mixed 聚合已落地：`backend/internal/service/node_subtree_aggregator.go:138`, `backend/internal/service/node_subtree_aggregator_test.go:151`, `backend/internal/service/node_subtree_aggregator_test.go:197`
- seed 工作流已改为纯命名端口图：`backend/internal/service/workflow_seed.go`, `backend/internal/service/workflow_seed_test.go:11`, `backend/internal/service/workflow_seed_test.go:97`
- DB 持久化 resume 已落地：`backend/internal/service/workflow_runner.go:381`
- phase4 `move-node` rollback 已落地：`backend/internal/service/node_move_node.go:146`, `backend/internal/service/node_phase4_processing_test.go:286`, `backend/internal/service/workflow_runner_test.go:876`
- 处理链 list-only 已落地：`backend/internal/service/node_category_router.go`（`categoryRouterExtractItems` 返回 2 值，无 isList）
- NodeSchema 统一使用 `Inputs`/`Outputs`：`backend/internal/service/workflow_runner.go:65`（已删除 `InputPorts`/`OutputPorts` 和 `NodeSchemaPort` 别名）
- AC-CLASS-2 关键词优先级：`backend/internal/service/workflow_phase3_e2e_test.go:TestEngineV2_AC_CLASS2_KeywordPriorityOverExtRatio`
- AC-PROC-1 处理链 E2E：`backend/internal/service/workflow_runner_test.go:TestEngineV2_AC_PROC1_ProcessingChainRenameAndMove`
- AC-ROLL-2/3 typed 格式 rollback：`backend/internal/service/node_phase4_processing_test.go:TestEngineV2_AC_ROLL2_*` / `TestEngineV2_AC_ROLL3_*`
- AC-ROLL-4 多节点逆序 rollback：`backend/internal/service/workflow_runner_test.go:TestEngineV2_AC_ROLL4_MultiNodeReverseRollback`
- AC-COMPAT-1 旧格式 rollback：`backend/internal/service/node_phase4_processing_test.go:TestEngineV2_AC_COMPAT1_LegacyOutputJsonRollbackCompat`
- AC-COMPAT-2 旧节点回归：`backend/internal/service/workflow_phase3_e2e_test.go:TestEngineV2_AC_COMPAT2_LegacyScanAndMoveJobRegression`
- backend 全量测试已通过：`CGO_ENABLED=0 go test ./...`
